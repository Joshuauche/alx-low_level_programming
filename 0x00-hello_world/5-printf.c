#include <stdioi.h>

/**
 * main - prints the string using printf function
 * Return: 0 if exited properly, non zero otherwise
 */
int main(void)
{
	/*prints the strings inputed*/
	printf("with proper grammar, but the outcome is a piece of art,");

	return (0);
}

