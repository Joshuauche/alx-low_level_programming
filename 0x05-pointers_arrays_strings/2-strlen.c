#include "holberton.h"
#include <string.h>

/**
 * _strlen - this return the string length of varaibles
 * @s: only parameter
 * Return: 0 always success
 */
int _strlen(char *s)
{
	return (strlen(s));
}

