#include "dog.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * print_dog - prints a struct dog
 * @d: dog variable pareameter
 */
void print_dog(struct dog *d)
{
	float age = age;
	char *name = name;
	char *owner = owner;

	if (d != NULL)
	{
		d->name = name;
		d->age = age;
		d->owner = owner;
	}
}

